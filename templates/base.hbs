<!DOCTYPE html>
<html lang="{{lang}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Aura-Scope Report</title>
    <style>
        :root {
            --primary-color: {{brand_color_primary}};
            --secondary-color: {{brand_color_secondary}};
            --text-color: #333;
            --bg-color: #f4f7f6;
            --card-bg: #ffffff;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background-color: var(--bg-color);
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: var(--card-bg);
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }
        header {
            border-bottom: 3px solid var(--primary-color);
            margin-bottom: 30px;
            padding-bottom: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        h1 { color: var(--primary-color); margin: 0; font-size: 24px; }
        .report-type { 
            text-transform: uppercase; 
            font-weight: bold; 
            letter-spacing: 1px; 
            font-size: 12px; 
            color: var(--secondary-color);
        }
        .summary {
            background: #eef2f7;
            padding: 20px;
            border-left: 5px solid var(--secondary-color);
            margin-bottom: 30px;
            font-style: italic;
        }
        section { margin-bottom: 40px; }
        h2 { border-left: 4px solid var(--primary-color); padding-left: 10px; font-size: 20px; color: var(--primary-color); }
        .item { margin-bottom: 25px; }
        .item-title { font-weight: bold; font-size: 18px; display: block; margin-bottom: 5px; color: var(--secondary-color); }
        .footer { 
            margin-top: 50px; 
            font-size: 12px; 
            text-align: center; 
            color: #888;
            border-top: 1px solid #eee;
            padding-top: 20px;
        }
        .pill {
            display: inline-block;
            padding: 4px 12px;
            background: var(--primary-color);
            color: white;
            border-radius: 20px;
            font-size: 11px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div>
                <h1>Aura-Scope</h1>
                <div class="report-type">{{report_type}} Report</div>
            </div>
            <div style="text-align: right;">
                <div style="font-size: 14px; font-weight: bold;">PR #{{pr_number}}</div>
                <div style="font-size: 12px; color: #666;">{{date}}</div>
            </div>
        </header>

        <div class="summary">
            {{summary}}
        </div>

        {{{body}}}

        <div class="footer">
            Generated by <strong>SQUAADS Aura-Scope</strong> • {{date}} • Project: {{project_name}}
        </div>
    </div>
</body>
</html>
