{
  "type": "OBJECT",
  "properties": {
    "welcome_message": { "type": "STRING" },
    "new_features": {
      "type": "ARRAY",
      "items": {
        "type": "OBJECT",
        "properties": {
          "feature_name": { "type": "STRING" },
          "benefit": { "type": "STRING" },
          "how_to_use": { "type": "STRING" }
        },
        "required": ["feature_name", "benefit", "how_to_use"]
      }
    },
    "improvements_and_fixes": {
      "type": "ARRAY",
      "items": { "type": "STRING" }
    }
  },
  "required": ["welcome_message", "new_features", "improvements_and_fixes"]
}
